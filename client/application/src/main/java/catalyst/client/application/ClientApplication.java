package catalyst.client.application;

import catalyst.client.engine.Launcher;

public class ClientApplication {
    public static void main(String[] args) {
        Launcher.run(ClientApplication.class, args);
    }
}
