catalyst:
  server:
    port: ${CATALYST_SERVER_PORT:35555}
    db-url: ${CATALYST_SERVER_DB_URL:${CATALYST_DB_URL}}
    db-user: ${CATALYST_SERVER_DB_USER:${CATALYST_DB_USER}}
    db-password: ${CATALYST_SERVER_DB_PASSWORD:${CATALYST_DB_PASSWORD}}
